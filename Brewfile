# System-level tools that mise/pnpm don't manage. Install with: brew bundle
# (Node and pnpm are pinned in mise.toml; Docker Desktop is assumed present
# for the integration test services.)

# GitHub-faithful markdown preview, used by `pnpm docs` / `make docs`.
brew "grip"
